import Block from '../../../features/block/Block';
import styles from './main.module.css';

const Answers = () => {
    return (
        <div>
            <Block />
        </div>
    );
}

export default Answers;